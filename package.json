{
  "name": "os-interiors-root",
  "version": "1.0.0",
  "description": "Root package.json for Render deployment",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "echo \"No build step required for this Node.js/Static application.\"",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.6.2"
  }
}
